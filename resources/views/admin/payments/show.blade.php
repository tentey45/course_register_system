@extends('layouts.app')
@section('title', 'Payment #' . $payment->id . ' — Admin SCRS')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h4 class="fw-bold mb-1">Payment #{{ $payment->id }}</h4><p class="text-muted mb-0">ABA PayWay transaction record and verification evidence.</p></div><div>@if(in_array($payment->status, ['pending', 'paid']))<form class="d-inline" method="POST" action="{{ route('admin.payments.confirm', $payment) }}">@csrf<button class="btn btn-success" onclick="return confirm('Confirm this payment after checking ABA and activate the registration?')">Confirm Registration</button></form>@endif <a class="btn btn-outline-secondary" href="{{ route('admin.payments.index') }}">Back to Payments</a></div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if(session('error'))<div class="alert alert-danger">{{ session('error') }}</div>@endif
<div class="row g-4"><div class="col-lg-7"><div class="card border-0 shadow-sm rounded-4 p-4"><h6 class="fw-bold mb-3">Transaction Details</h6><dl class="row mb-0"><dt class="col-sm-5">Payment status</dt><dd class="col-sm-7"><span class="badge {{ $payment->status === 'paid' ? 'bg-success' : ($payment->status === 'pending' ? 'bg-warning text-dark' : 'bg-danger') }}">{{ ucfirst($payment->status) }}</span></dd><dt class="col-sm-5">Transaction ID</dt><dd class="col-sm-7 font-monospace small">{{ $payment->transaction_id ?? '—' }}</dd><dt class="col-sm-5">Amount</dt><dd class="col-sm-7 fw-bold">${{ number_format($payment->amount, 2) }} {{ $payment->currency }}</dd><dt class="col-sm-5">Method</dt><dd class="col-sm-7">{{ $payment->payment_method ?? $payment->method ?? '—' }}</dd><dt class="col-sm-5">Paid at</dt><dd class="col-sm-7">{{ $payment->paid_at?->format('d M Y, H:i') ?? '—' }}</dd><dt class="col-sm-5">Registration</dt><dd class="col-sm-7">{{ $payment->registration?->status ?? '—' }}</dd></dl></div></div><div class="col-lg-5"><div class="card border-0 shadow-sm rounded-4 p-4"><h6 class="fw-bold mb-3">Student & Course</h6><p class="mb-1"><strong>Student:</strong> {{ $payment->student?->name }}</p><p class="text-muted small">{{ $payment->student?->student_id }} · {{ $payment->student?->email }}</p><p class="mb-1"><strong>Course:</strong> {{ $payment->course?->course_code }} — {{ $payment->course?->title }}</p><p class="text-muted small mb-0">{{ $payment->course?->department?->name }} · {{ $payment->course?->semester?->name }}</p></div></div><div class="col-12"><div class="card border-0 shadow-sm rounded-4 p-4"><h6 class="fw-bold">Gateway Response</h6><pre class="bg-light rounded p-3 mb-0 small" style="max-height:320px;overflow:auto">{{ json_encode($payment->payment_response ?? $payment->gateway_response ?? [], JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) }}</pre></div></div></div>
@endsection
