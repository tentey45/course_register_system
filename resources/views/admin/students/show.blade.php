@extends('layouts.app')
@section('title', 'Student Profile — Admin SCRS')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h4 class="fw-bold mb-1">{{ $student->name }}</h4><p class="text-muted mb-0">{{ $student->student_id }} · {{ $student->department?->name }}</p></div><div><a class="btn btn-outline-primary" href="{{ route('admin.students.edit', $student) }}">Edit</a>@if($student->is_active)<form class="d-inline" method="POST" action="{{ route('admin.students.deactivate', $student) }}">@csrf<button class="btn btn-outline-danger" onclick="return confirm('Disable this student account?')">Disable</button></form>@else<form class="d-inline" method="POST" action="{{ route('admin.students.activate', $student) }}">@csrf<button class="btn btn-success">Activate</button></form>@endif</div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="row g-4"><div class="col-lg-4"><div class="card border-0 shadow-sm rounded-4 p-4"><h6>Student Information</h6><dl class="row small mb-0"><dt class="col-5">Email</dt><dd class="col-7">{{ $student->email }}</dd><dt class="col-5">Status</dt><dd class="col-7">{{ $student->is_active ? 'Active' : 'Disabled' }}</dd><dt class="col-5">Joined</dt><dd class="col-7">{{ $student->created_at?->format('d M Y') }}</dd></dl></div></div><div class="col-lg-8"><div class="card border-0 shadow-sm rounded-4 overflow-hidden"><div class="p-3 border-bottom fw-semibold">Registration History</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Course</th><th>Status</th><th>Registered</th></tr></thead><tbody>@forelse($student->registrations as $registration)<tr><td>{{ $registration->course?->course_code }} — {{ $registration->course?->title }}</td><td>{{ str_replace('_', ' ', ucfirst($registration->status)) }}</td><td>{{ $registration->registered_at?->format('d M Y') }}</td></tr>@empty<tr><td colspan="3" class="text-muted">No registrations.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
